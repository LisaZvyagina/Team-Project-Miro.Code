# Проект: Кодирование/декодирование Base64

Этот проект реализует алгоритмы **кодирования** и **декодирования** формата Base64 согласно стандарту RFC 4648.

---

## 📌 Цель работы

Изучить алгоритм кодирования и декодирования Base64 и разработать приложение, реализующее оба механизма.

---

## 📁 Содержание

1. Цель работы
2. Компиляция программы
3. Команды для кодирования и декодирования
4. Практические примеры
5. Base64 алфавит
6. Контрольные задания
7. Примечания

---

## 🔧 Компиляция программы

Сохраните код в файл `base64.cpp`, затем используйте команду компиляции для вашей системы.

### Windows (MinGW)

```bash
g++ -o base64.exe base64.cpp
```

### Linux / macOS

```bash
g++ -o base64 base64.cpp
```

### Рекомендуемая оптимизация

```bash
g++ -O2 -o base64.exe base64.cpp
```

---

## 🔐 Команды для кодирования и декодирования

---

### Универсальная команда `base64`

Утилита `base64` - основная команда для кодирования и декодирования. Она поддерживает работу с файлами и стандартным вводом/выводом.

### Справка

```bash
base64 --help
base64 -h
```

### Примеры использования

#### Кодирование строки:

```bash
base64 encode "Hello World"
base64 -e "Secret data"
```

#### Декодирование строки:

```bash
base64 decode "SGVsbG8gV29ybGQ="
base64 -d "U2VjcmV0IGRhdGE="
```

#### Работа с файлами:

```bash
# Кодирование файла
base64 -e -f input.txt -o encoded.b64
base64 encode input.txt encoded.b64

# Декодирование файла
base64 -d -f encoded.b64 -o decoded.txt
base64 decode encoded.b64 decoded.txt
```

#### Стандартный ввод/вывод:

```bash
# Кодирование из stdin
echo "Hello" | base64 encode
cat file.txt | base64 -e

# Декодирование в stdout
echo "SGVsbG8=" | base64 decode
```

### Параметры командной строки

```
--encode, -e        Кодировать данные
--decode, -d        Декодировать данные
--file, -f ФАЙЛ     Входной файл (если не указан, используется stdin)
--output, -o ФАЙЛ   Выходной файл (если не указан, используется stdout)
--help, -h          Показать справку
```

---

## 📘 Примеры использования

### Кодирование текста

```
Input:  Hello World
Output: SGVsbG8gV29ybGQ=
```

```
Input:  Привет мир
Output: 0J/RgNC40LLQtdGCLdC80LjRgA==
```

### Декодирование текста

```
Input:  SGVsbG8gV29ybGQ=
Output: Hello World
```

```
Input:  QWJm
Output: Abf
```

### Кодирование файла

```bash
# Кодирование текстового файла
base64 -e -f document.txt -o document.b64

# Кодирование бинарного файла
base64 -e -f image.jpg -o image.b64
```

### Декодирование файла

```bash
# Декодирование в текстовый файл
base64 -d -f document.b64 -o document.txt

# Декодирование в бинарный файл
base64 -d -f image.b64 -o image.jpg
```

---

## 🔤 Base64 алфавит

```
ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/
```

- A-Z: 0-25
- a-z: 26-51
- 0-9: 52-61
- +: 62
- /: 63
- =: символ заполнения (padding)

---

## 📝 Контрольные задания

Проект включает реализацию функций для решения контрольных заданий:

### Задание 1: Кодирование
```
Input:  Xz25
Output: WHoyNQ==
```

### Задание 2: Кодирование
```
Input:  Fh7k
Output: Rmg3aw==
```

### Задание 3: Кодирование
```
Input:  gRn4
Output: Z1JuNA==
```

### Задание 4: Декодирование
```
Input:  Pt54
Output: {результат вычисляется программой}
```

### Задание 5: Декодирование
```
Input:  Rj0v
Output: {результат вычисляется программой}
```

### Задание 6: Декодирование
```
Input:  zbTM
Output: {результат вычисляется программой}
```

*Для проверки результатов запустите программу с соответствующими тестовыми данными.*

---

## ⚙️ Тестирование программы

### Базовые тесты

```bash
# Тест кодирования
echo -n "Abf" | base64 encode
# Ожидаемый результат: QWJm

# Тест декодирования
echo -n "QWJm" | base64 decode
# Ожидаемый результат: Abf
```

### Автоматическое тестирование

```bash
# Создание тестового файла
echo "Test data for Base64 encoding" > test.txt

# Кодирование и декодирование
base64 encode test.txt encoded.b64
base64 decode encoded.b64 decoded.txt

# Проверка совпадения
diff test.txt decoded.txt
# Если файлы идентичны, команда не выведет ничего
```

---

## 📊 Примечания

1. **Размер данных**: При кодировании Base64 объем данных увеличивается примерно на 33%
2. **Использование**: Base64 широко применяется в:
   - Электронной почте (MIME)
   - Веб-разработке (Data URLs)
   - Хранении бинарных данных в JSON/XML
   - Системах аутентификации
3. **Символы заполнения**: Символ `=` добавляется только в конец строки, если количество исходных байт не кратно 3
4. **Перенос строк**: Некоторые реализации добавляют переносы строк каждые 76 символов (по стандарту MIME)
5. **Кодировка**: Проект поддерживает UTF-8 для текстовых данных

---

## 🔗 Ссылки

- [RFC 4648 - The Base16, Base32, and Base64 Data Encodings](https://tools.ietf.org/html/rfc4648)
- [Base64 на Википедии](https://ru.wikipedia.org/wiki/Base64)
- [MIME стандарты](https://ru.wikipedia.org/wiki/MIME)

---

## 📄 Лицензия

Учебный проект для лабораторной работы №4 по дисциплине "Программирование". Разрешено использование в учебных целях.

---

## 👥 Авторы

*Студенческая группа, выполняющая лабораторную работу №4*

---

## 📅 Дата

*Дата выполнения работы*
